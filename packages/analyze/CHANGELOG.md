# Changelog

## [0.1.0] - Initial release

### Features

- `analyzeProfile` — analyze a Spark profiler report
- `analyzeTimings` — analyze a Paper timings report
- `collector` — report analyzed IDs to a collector endpoint
- Full config rule sets for bukkit, spigot, paper (timings & profile), pufferfish, purpur, and plugins
